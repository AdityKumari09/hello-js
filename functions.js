function starPattern(rows = 5) {
    for (let i = 0; i < rows; i++){
        console.log("* ");
    }
}

starPattern(5)
starPattern()

let add = (number) => { return number +  number};
let result = add(5)
console.log(result)
